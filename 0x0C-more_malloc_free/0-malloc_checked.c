#include "main.h"
#include <stdlib.h>

/**
 * malloc_checked - Allocates memory
 * @b: the size to allocate
 *
 * Return: nothing
 */
void *malloc_checked(unsigned int b)
{
	void *mem = malloc(b);

	mem = malloc(b);

	if (y == NULL)
		exit(98);

	return (mem);
}
